import SwiftUI

struct RecipesPlaceholderView: View {
    var body: some View {
        PhasePlaceholderView(
            phase: 3,
            feature: "Recipes",
            blurb: "Create and manage your high-protein recipes with ingredients, photos, and auto-computed macros in Phase 3."
        )
    }
}
