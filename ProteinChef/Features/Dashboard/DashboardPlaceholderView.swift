import SwiftUI

struct DashboardPlaceholderView: View {
    var body: some View {
        PhasePlaceholderView(
            phase: 4,
            feature: "Today",
            blurb: "Daily protein, carbs, fat, and calorie dashboard — plus high-protein recipe suggestions — arrives in Phase 4."
        )
    }
}
