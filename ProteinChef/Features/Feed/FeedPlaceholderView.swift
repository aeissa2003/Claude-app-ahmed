import SwiftUI

struct FeedPlaceholderView: View {
    var body: some View {
        PhasePlaceholderView(
            phase: 6,
            feature: "Friends",
            blurb: "Mutual-accept friends, recipe posts, likes, comments, and save-a-copy land in Phase 6."
        )
    }
}
